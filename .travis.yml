sudo: required

language: node_js

git:
    submodules: false

before_install:
    - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules

services:
  - docker

install: make install

script: make build

after_success:
  - make publish-gh-pages
  - make update-modules
